import adminActionTypes from "redux/admin/admin.types";

const INITIAL_STATE = {};

const adminReducer = (state = INITIAL_STATE, { type, pyload }) => {
  switch (type) {
    default:
      return state;
  }
};

export default adminReducer;
